language: python
python:
  - "2.6"
  - "2.7"

addons:
  postgresql: "9.3"

before_install:
    - npm install -g less
    - mkdir -p $HOME/socorro-install
    - export PREFIX=$HOME/socorro-install

install:
  - make install

script:
  - make test

notifications:
  email: false
